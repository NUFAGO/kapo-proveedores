'use client';

// Mantener compatibilidad con el código existente
// Este archivo ahora redirige al contexto de admin
export { useAuth } from './auth-admin-context';
export { AuthAdminProvider as AuthProvider } from './auth-admin-context';
export type { AdminUser as User } from './auth-admin-context';
